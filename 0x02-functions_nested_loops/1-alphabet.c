#include "main.h"
void print_alphabet(void)
/**
 * main - prints alphabet in lowercase followed by new line
 * Return: Always 0
 */

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)

		putchar("ch");
	putchar('\n');
	print_alphabet();
	return (0);
}
