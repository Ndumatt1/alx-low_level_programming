#include "main.h"
#include <string.h>

/**
 * _strcat - concatenates two strings
 *
 * @dest: destination
 * @src: string to be moved
 * Return: dest
 */

char *_strcat(char *dest, char *src)
{
	_strcat(dest, src);

	return (dest);
}
